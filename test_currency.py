from currency_convert import *

rates = [("USD", "EUR", 1.25)]

def test_convert_match():
    assert convert(2, 1, "COS", "COS") == 1




#rates = [("USD", "EUR", .74)]

#def convert(rates, value, orig_cur, new_cur):
